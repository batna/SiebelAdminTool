{% extends "BatnaArchiSiebelBundle::layout.html.twig" %}

{% block title %}Caractéristiques d'une machine - {{ parent() }}{% endblock %}
{% block batnaarchi_body %}


<div class="mws-panel grid_8">
        <div class="mws-panel-header">
                <span class="mws-i-24 i-clipboard-1">Machine {{ entity.hostname }}</span>
        </div>
        <div class="mws-panel-toolbar top clearfix">
                <ul>
                        <li><a href="{{ path('machine_new') }}" class="mws-ic-16 ic-page">Nouvelle machine</a></li>
                        <li><a href="{{ path('machine_edit', { 'id': entity.id }) }}" class="mws-ic-16 ic-edit">Modifier</a></li>
                        <li><a href="{{ path('machine_delete', { 'id': entity.id }) }}" class="mws-ic-16 ic-cross">Supprimer</a></li>
                        <li><a href="{{ path('machine') }}" class="mws-ic-16 ic-application-view-list">Voir la liste</a></li>
                </ul>
        </div>
   
    <div class="mws-panel-body" style="padding:10px">


<table class="mws-table">
    <tbody>
        <tr>
            <th>Hostname</th>
            <td>{{ entity.hostname }}</td>
        </tr>
        <tr>
            <th>IP</th>
            <td>{{ entity.ip }}</td>
        </tr>
        <tr>
            <th>Type</th>
            <td>{{ entity.type }}</td>
        </tr>
        <tr>
            <th>OS</th>
            <td>{{ entity.os }}</td>
        </tr>
        <tr>
            <th>RAM</th>
            <td>{{ entity.ram }}</td>
        </tr>
        <tr>
            <th>Core #</th>
            <td>{{ entity.cpuCore }}</td>
        </tr>
        <tr>
            <th>CPU GHz</th>
            <td>{{ entity.cpuFrequency }}</td>
        </tr>
        <tr>
            <th>Parent</th>
            <td>{{ entity.parent }}</td>
        </tr>
    </tbody>
</table>
    </div>
</div>
{% endblock %}
